platform :ios, '11.0'
use_frameworks! :linkage => :static

target 'RouteToARM64SimSampleProject' do
  pod 'Libyuv', '1703' # static fat lib
  pod 'MNN'            # binary fat static framework
  pod 'mob_sharesdk'   # binary fat dynamic framework + static framework
end
